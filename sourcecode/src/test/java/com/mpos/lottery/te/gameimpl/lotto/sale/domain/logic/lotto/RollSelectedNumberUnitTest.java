package com.mpos.lottery.te.gameimpl.lotto.sale.domain.logic.lotto;

import org.junit.Test;

public class RollSelectedNumberUnitTest {

    @Test
    public void testValidateAndAssembleEntry() throws Exception {
        // TODO
        // LottoTicket ticket = new LottoTicket();
        // LottoOperationParameter lop = new LottoOperationParameter();
        // ticket.setLottoOperatorParameter(lop);
        //
        // BetOptionStrategy s = mock();
        // LottoEntry entry = new LottoEntry();
        // entry.setSelectNumber("1,4,11,15");
        // s.validateAndAssembleEntry(ticket, entry);
        // assertEquals(new Combination(45, 2).getTotal().longValue(), entry.getTotalBets(), 0);
        //
        // try {
        // entry = new LottoEntry();
        // entry.setSelectNumber("1,4,11,15,16,17,18");
        // s.validateAndAssembleEntry(ticket, entry);
        // }
        // catch (ApplicationException e) {
        // assertEquals(SystemException.CODE_WRONGFORMAT_SELECTEDNUMBER, e.getErrorCode());
        // }
    }

    // private RollStrategy mock() {
    // MLotteryContext prop = MLotteryContext.getInstance();
    // String numberFormat = prop.getSelectedNumberFormat(Game.TYPE_LOTT, LottoEntry.BETOPTION_ROLL);
    // LottoFunType funType = DomainMocker.mockLottoFunType();
    // return new RollStrategy(numberFormat, funType);
    // }
}
